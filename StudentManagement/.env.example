# Example .env for local development
# Replace username/password/host/database accordingly. Do NOT commit real credentials.
NEON_CONNECTION=postgresql://<username>:<password>@<host>:5432/<database>?sslmode=require
